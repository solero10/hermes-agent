# HANDOFF — 2026-06-21 13:40:30 PDT

## Goal of the project

Add three Otter transcripts that have already been processed through Panning for Gold to the read-only OpenBrain ingestion dashboard, using the existing dashboard snapshot/exporter path rather than re-running Panning or writing new CortexDB/OpenBrain memories.

## Current status

- Ken invoked `/plan` for: “Take 3 Otter transcripts that have already been processed through panning for gold and add to dashboard.”
- Planning only was completed. No dashboard snapshot was published and no CortexDB/OpenBrain live capture was performed.
- Plan saved in the active repo workspace:
  - `/home/kernk/.hermes/hermes-agent/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md`
- Plan copied to Ken’s canonical Hermes plans folder for File Browser access:
  - `/home/kernk/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md`
- OpenBrain/CortexDB memory was searched for relevant dashboard/Panning context and a compact task memory was captured after plan creation.
- Exact context-compression count is not visible in this API session, but Ken invoked the handoff skill, so this handoff was created directly.

## Files changed

Created:

- `HANDOFF_2026-06-21_134030_PDT.md` — this handoff file in the project root.
- `.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md` — implementation plan in the active repo workspace.
- `/home/kernk/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md` — File Browser-served copy of the plan.

Observed repository status for relevant dashboard files:

- `plugins/openbrain_ingestion/dashboard/plugin_api.py` is untracked in git.
- `plugins/openbrain_ingestion/dashboard/snapshot_exporter.py` is untracked in git.
- `tests/plugins/test_openbrain_ingestion_dashboard_panning_exporter.py` is untracked in git.
- `website/docs/user-guide/features/openbrain-ingestion-dashboard.md` is untracked in git.

Those untracked files existed as part of the current OpenBrain dashboard work. Do not assume a clean tree.

## Commands and tool checks run

Read/loaded skills:

- `panning-for-gold`
- `personal-knowledge-data-operations`
- `otter-export-package`
- `filebrowser-url-linking`
- `response-wrapup-protocol`
- `handoff-after-context-compression` was supplied by Ken in the prompt.

Repository/context inspection:

```bash
# Repo file discovery
search_files('*openbrain*', target='files', path='.')
search_files('SnapshotV1|snapshot_exporter|source-types|Panning|OpenBrain', target='content', path='.', file_glob='*.py')
search_files('*openbrain*dashboard*plan*|*dashboard*', target='files', path='.hermes/plans')

# Timestamp for plan file naming
TZ=America/Los_Angeles date '+%Y-%m-%d_%H%M%S_%Z'
# Returned: 2026-06-21_133216 PDT
```

Read important files:

- `plugins/openbrain_ingestion/dashboard/snapshot_exporter.py`
- `plugins/openbrain_ingestion/dashboard/plugin_api.py`
- `tests/plugins/test_openbrain_ingestion_dashboard_panning_exporter.py`
- `website/docs/user-guide/features/openbrain-ingestion-dashboard.md`
- Selected source Panning artifacts under:
  - `/mnt/d/LocalRuntime/project-runs/agentic-workflows/otter-panning-for-gold/otter-ledger-ready-21-20260620-174403Z/artifacts/`

Verified exporter can load the full source run:

```python
# Used execute_code to import snapshot_exporter.py and call build_snapshot_from_panning_run()
run_root = Path('/mnt/d/LocalRuntime/project-runs/agentic-workflows/otter-panning-for-gold/otter-ledger-ready-21-20260620-174403Z/artifacts')
# Result: 21 source units, 130 thoughts, source_types=[{'id':'transcripts','label':'Transcripts','count':21}]
```

Verified default three-source selection counts:

```json
{
  "selected_source_ids": [
    "otter:GOJu7wHKdLQ1KVQ-IvkQP3zDDro",
    "otter:Wr4QMz7tI4qgG5jwaIs6Vlh7v6c",
    "otter:uH2qJPIXzrqyjgoS6j1YrjRZjI4"
  ],
  "selected_titles": [
    "2026-01-02 Better You - Healthcare Insurance Navigation",
    "2026-01-07 Marriage and Relocation Challenges",
    "2025-12-30 Family Relationship Counseling"
  ],
  "source_items": 3,
  "inventory.jsonl": 32,
  "dedupe-receipts.jsonl": 32,
  "capture-candidates.jsonl": 25
}
```

File Browser URL command run:

```bash
~/.local/bin/filebrowser-url-for-path '/home/kernk/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md'
```

Git status check run for relevant files:

```bash
git status --short -- .hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md plugins/openbrain_ingestion/dashboard/snapshot_exporter.py plugins/openbrain_ingestion/dashboard/plugin_api.py tests/plugins/test_openbrain_ingestion_dashboard_panning_exporter.py website/docs/user-guide/features/openbrain-ingestion-dashboard.md
```

Output:

```text
?? plugins/openbrain_ingestion/dashboard/plugin_api.py
?? plugins/openbrain_ingestion/dashboard/snapshot_exporter.py
?? tests/plugins/test_openbrain_ingestion_dashboard_panning_exporter.py
?? website/docs/user-guide/features/openbrain-ingestion-dashboard.md
```

Handoff timestamp command:

```bash
TZ=America/Los_Angeles date '+%Y-%m-%d_%H%M%S_%Z'
# Returned: 2026-06-21_134030_PDT
```

## Important decisions

- Use the existing Panning run as source of truth:
  - `/mnt/d/LocalRuntime/project-runs/agentic-workflows/otter-panning-for-gold/otter-ledger-ready-21-20260620-174403Z/artifacts`
- Default to the first three ready source units from that run because Ken did not specify exact titles.
- Do not re-run Panning.
- Do not write live CortexDB/OpenBrain memories.
- Do not update the OpenBrain source-ingestion ledger; this is a dashboard visualization update only.
- Create a filtered staging run with only selected `source_id` rows, then feed that staging run into `snapshot_exporter.export_dashboard_snapshot()`.
- Publish to `~/.hermes/openbrain-ingestion-dashboard/snapshot.json` only after preview validation and backup.
- Do not copy `panning-run.sqlite` into the staging run because it may contain stale full-run context. The existing exporter works without it.

## Known errors or failed attempts

- The live dashboard snapshot path did not exist during planning:
  - `/home/kernk/.hermes/openbrain-ingestion-dashboard` was not found.
  - This is not a blocker; Task 5 in the plan creates it if needed.
- `.hermes/plans` under the repo was not found during an initial search before the plan was created.
- No implementation/test failure occurred because the request was plan-only.
- No browser/dashboard server was started for this plan-only turn.

## Exact next steps

Use the saved plan as the source of truth:

- `/home/kernk/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md`

Recommended execution order:

1. Run Task 1 from the plan to verify the source run and selected three transcripts still match expected counts.
2. Run Task 2 tests:

```bash
scripts/run_tests.sh \
  tests/plugins/test_openbrain_ingestion_dashboard_panning_exporter.py \
  tests/plugins/test_openbrain_ingestion_dashboard_plugin.py \
  tests/plugins/test_openbrain_ingestion_dashboard_mount.py \
  tests/plugins/test_openbrain_ingestion_dashboard_bundle.py
```

3. Run Task 3 to create the filtered staging run under:

```text
/mnt/d/LocalRuntime/project-runs/agentic-workflows/openbrain-dashboard/otter-three-dashboard-20260621-133216PDT/panning-run
```

4. Run Task 4 to export and validate preview snapshot:

```text
/mnt/d/LocalRuntime/project-runs/agentic-workflows/openbrain-dashboard/otter-three-dashboard-20260621-133216PDT/snapshot.preview.json
```

5. Run Task 5 to publish atomically to:

```text
/home/kernk/.hermes/openbrain-ingestion-dashboard/snapshot.json
```

6. Run Task 6 API smoke using FastAPI TestClient.
7. Run Task 7 browser smoke at:

```text
http://127.0.0.1:9119/openbrain
```

8. If validation fails, run Task 8 rollback.
9. Report exactly what was published, counts, paths, and whether UI/API validation passed.

## Warnings or assumptions

- Ken may have meant three specific Otter transcripts. If so, replace the `SELECTED_SOURCE_IDS` list in the plan before executing Task 3.
- Publishing the subset snapshot replaces the current single dashboard snapshot view. Back up first.
- The source transcripts include private healthcare/family context. Do not paste raw transcript text into chat.
- The dashboard is read-only, but the snapshot still contains sensitive derived summaries. Keep it local/private.
- Some candidates are `degraded_created`/review-oriented Panning outputs, not necessarily live CortexDB memories. Do not describe all cards as imported.
- The current repo has untracked OpenBrain dashboard files. Avoid accidental cleanup, commit, or overwrite unless Ken explicitly asks.
- If execution reveals the exporter has gaps on the real filtered artifacts, stop and make a new code-change plan rather than patching during an execution run without review.

## Ready-to-paste resume prompt for a new Hermes session

```text
Resume from /home/kernk/.hermes/hermes-agent/HANDOFF_2026-06-21_134030_PDT.md.

Task: execute the saved plan to add three already-processed Otter/Panning transcripts to the OpenBrain ingestion dashboard. Use the plan at /home/kernk/.hermes/plans/2026-06-21_133216-otter-panning-dashboard-three-transcripts.md as source of truth. Do not re-run Panning and do not perform live CortexDB/OpenBrain capture. First verify the selected source IDs/counts, run the existing OpenBrain dashboard exporter/plugin tests, create the filtered staging run, validate a preview SnapshotV1, back up any existing ~/.hermes/openbrain-ingestion-dashboard/snapshot.json, publish the new snapshot atomically, then verify API and browser UI at /openbrain. Report counts, paths, backup, and any caveats. If Ken meant different three transcripts, stop and ask for the three titles/source IDs before publishing.
```
